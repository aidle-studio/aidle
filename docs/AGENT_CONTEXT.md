# AGENT_CONTEXT.md

## 現在フェーズ
- Phase: 実装準備完了（仕様確定後）
- 直近ゴール: `TC-001` から Red テストを開始する

## プロジェクト方針（要点）
- 本プロジェクトは AIDD を前提とし、仕様駆動（SDD）とテスト駆動（TDD）を統合して進める。
- 実装前に、対象仕様は必ず合意済みであること。
- 仕様は `REQ-*` / `AC-*`、テストは `TC-*` で追跡する。

## 優先ドキュメント読了順
1. `docs/RULES.md`
2. `docs/SPEC.md`
3. `docs/TEST_PLAN.md`
4. `docs/TODO.md`
5. `docs/KNOWLEDGE.md`

## 実行ルール
- 変更時は必ず対応する `TC-*` を先に Red で追加する。
- 実装は最小変更で Green にし、その後 Refactor する。
- 例外運用（仕様逸脱、カバレッジ未達、タイムアウト例外）は理由をPRに明記する。

## 次アクション
1. `TC-001` / `TC-002` の Red テストを追加する
2. `aidle init` の最小実装で Green を通す
3. ロールバック・エラー契約のテストへ進む
