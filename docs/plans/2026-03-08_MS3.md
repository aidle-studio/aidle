# MS3 Plan (2026-03-08)

## Plan Meta
- Milestone ID: MS3
- Milestone Name: CLIオプションと設定適用の完成
- Date: 2026-03-08
- Owner: OLBuGames / Codex
- Status: Done

## Goal
- `SPEC.md` で定義した主要オプションと `aidle.toml` キーを実装し、対応する契約テストを追加して Green 化する。

## Scope
- In Scope:
  - `--json`, `--non-interactive`, `--output`, `--verbose`, `--template`, `--agent-format`
  - `aidle.toml` の未実装キー反映（`template.name`, `agent.format`, `execution.non_interactive`, `execution.verbose`, `execution.json`）
  - `TC-014`〜`TC-018` の追加と Green 化
- Out of Scope:
  - テンプレートの実体分離（MS4）
  - CIへのカバレッジ組み込み（MS5）

## Tasks
- [x] T1: `--json` + `execution.json` 実装、`TC-014` 追加/Green
- [x] T2: `--non-interactive` + `execution.non_interactive` 実装、`TC-015` 追加/Green
- [x] T3: `--output` + `project.output` 優先順位整備、`TC-016` 追加/Green
- [x] T4: `--template` + `template.name` 実装、`TC-017` 追加/Green
- [x] T5: `--agent-format` + `agent.format` 実装、`TC-018` 追加/Green
- [x] T6: `--verbose` + `execution.verbose` 実装（ログレベル分離）
- [x] T7: `docs/TEST_PLAN.md` へ `TC-014`〜`TC-018` を正式追記
- [x] T8: MS3の達成判定を `docs/TODO.md` へ反映

## Iteration Plan
1. Iteration 1: `--json` / `--non-interactive` をテスト先行で実装（T1, T2）
2. Iteration 2: `--output` / `--verbose` を実装（T3, T6）
3. Iteration 3: `--template` / `--agent-format` と config キー反映（T4, T5）
4. Iteration 4: テスト計画更新と達成判定（T7, T8）

## Agreement
- 合意日: 2026-03-08
- 合意内容: 本プランでMS3を反復実行する
- 合意者: せんぱい

## Execution Log
- 2026-03-08: Plan合意。Iteration 1 に着手。
- 2026-03-08: Iteration 1〜3 実装完了（`--json`, `--non-interactive`, `--output`, `--verbose`, `--template`, `--agent-format`, config反映）。
- 2026-03-08: Iteration 4 完了（`TC-014`〜`TC-018` 正式追記、MS3達成判定更新）。

## Re-plan Notes
- (empty)
