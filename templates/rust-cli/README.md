# Rust CLI Template

このテンプレートは Rust CLI プロジェクト向けの初期構成です。

## 目的
- `cargo` ベースのCLI開発をすぐ開始できる状態を作る。
- AIDD/TDD運用に必要な `docs/` と `AGENTS.md` を同時に整備する。

## 開始手順
1. `cargo init --bin` でCLI骨格を作成する
2. `cargo nextest run` でテストを実行する
3. `docs/SPEC.md` と `docs/TEST_PLAN.md` に沿って実装を進める
