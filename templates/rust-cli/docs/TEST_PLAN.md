# TEST_PLAN.md

このファイルは、仕様書（`SPEC.md`）で定義した受け入れ基準（`AC-*`）を、テスト駆動開発（TDD）で実装可能なテストケース（`TC-*`）に変換して管理する計画書です。

## 1. 目的
`SPEC.md` の要求がすべてテストでカバーされていることを保証し、実装状況（Red / Green）を可視化すること。

## 2. トレーサビリティ（AC -> TC）
[TODO: SPEC.md の AC-* に対して、どの TC-* でテストするかを紐付ける]

- `AC-001` -> `TC-001`, `TC-002`
- `AC-002` -> `TC-003`

## 3. テストケース一覧 (Test Cases)

[TODO: テストケースを定義する。AIは実装を開始する前に、必ずここに対応する TC-* を `Red` の状態で追加し、実装後に `Green` に更新すること。]

| TC ID | 対応AC | レベル | 観点 | 状態 |
|---|---|---|---|---|
| TC-001 | AC-001 | unit | [テストの目的や確認する振る舞い] | Red |
| TC-002 | AC-001 | integration | [テストの目的や確認する振る舞い] | Red |
| TC-003 | AC-002 | e2e | [テストの目的や確認する振る舞い] | Red |

*※レベルの例: `unit` (単体), `integration` (結合), `e2e` (E2E)*
*※状態の例: `Red` (未実装/失敗), `Green` (成功)*

## 4. 実装順序（推奨）
[TODO: どのテスト（TC）から順番に実装（Red -> Green -> Refactor）を進めるかの計画を記載する]

1. `TC-001` (コア機能から着手)
2. `TC-002`
3. `TC-003` (境界値やエラー系のテスト)

## 5. テスト実行方法
[TODO: テストを実行するためのコマンドを記載する]
- 実行コマンド: `[TODO: 例: npm run test, cargo nextest run]`
- 特定テストの実行: `[TODO: 例: npm run test -- -t "テスト名"]`
