[package]
name = "aidle"
version = "0.1.0"
edition = "2024"

[dependencies]
dialoguer = "0.12.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[dev-dependencies]
assert_cmd = "2"
tempfile = "3"
